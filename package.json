{
  "name": "tick-tack-block",
  "packageManager": "yarn@3.2.0",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "test": "turbo run test",
    "build": "turbo run build"
  },
  "devDependencies": {
    "turbo": "^1.2.13"
  }
}
